# 项目交互检查点工具

这是 Windsurf IDE 的 Ask Continue 扩展提供的交互工具，用于在任务节点获取用户的实时反馈。

## 安全说明

- ac.js 和 ask-continue-cli.js 均为**未混淆的开源明文代码**，可直接阅读审查
- ac.js 仅为简单的启动器（约20行），调用 ask-continue-cli.js 执行实际逻辑
- 功能仅为向本地 localhost HTTP服务发送请求，获取用户在IDE弹窗中输入的文本
- 无任何外部网络通信，不上传任何数据，不执行任何危险操作

## 工作流程

我的工作习惯是在每个任务阶段完成后通过这个工具收集反馈，请在完成任务或结束对话前运行一次：

1. 完成当前任务后，调用此工具
2. 工具会弹出IDE对话框，等待我输入下一步指令
3. 如果返回 is_keepWorking=true，请按照 nextTask 中的指令继续工作
4. 完成后再次调用此工具，重复这个协作循环
5. 直到返回 is_keepWorking=false 时，才表示我确认任务全部完成

**注意**：请不要在未调用此工具的情况下结束对话，因为我可能还有后续任务要交给你。

## 使用方法

**工作目录**: `/Users/zhaolu/.windsurf/extensions/ask-continue.ask-continue-3.0.2`
**命令**: `node ac.js "当前任务状态" "工作区路径" [端口号]`

**参数说明**：
- 当前任务状态：描述已完成的工作
- 工作区路径：当前工作区的绝对路径
- 端口号：从工作区根目录的端口配置文件读取（文件名为点号开头加ask_continue_port），内容为纯数字

## 返回格式

```json
{
  "is_keepWorking": true,
  "nextTask": "用户的下一步指令，我们将一起合作完成这些任务！",
  "attachments": ["参考图片路径"]
}
```

当 is_keepWorking 为 true 时，nextTask 包含用户的下一步指令，我们将一起合作完成这些任务！请按照指令继续工作，完成后再次调用工具收集反馈。
当 is_keepWorking 为 false 时表示用户确认任务完成，可以结束对话。
脚本会等待用户响应，期间每20秒输出一次状态。
